#include "cmd.h"
#include "item.h"

#include <stdio.h>

/*
static int fsort(array_t *array)
{
    for (int i = array->size, last = i; i; i--) {
        item_t *item = a_get(array, i);
    }

    return 0;
}
*/

int cmd_sort(int argc, const char **argv, array_t *array)
{
    return 0;
}
